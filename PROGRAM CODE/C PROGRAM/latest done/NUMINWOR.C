#include<stdio.h>
#include<conio.h>
#include<string.h>

void convert(char)
void main()
{
	char *num;
	int ln;

	printf("\nEnter the digit pls\n");
	gets(num);
	ln=strlen(num);
	switch(ln)
	{
		case 1:
				convert(